source "https://rubygems.org"

ruby ">= 3.0"

# HTTP client for JSON-RPC calls. Lightweight, no global state.
gem "faraday", "~> 2.9"

# Loads .env into ENV so reviewers can drop a local .env with their RPC API
# key instead of exporting variables per shell session.
gem "dotenv", "~> 3.1"
